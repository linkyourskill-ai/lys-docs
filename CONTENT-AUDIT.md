# Course ↔ Platform Content Audit

Factual-drift ledger. **Report-only** — no lesson file was changed. Verdicts follow the source-of-truth precedence: platform docs are the reference; where a doc contradicts the live-verified platform ground truth, the gap is a **SOURCE-CONFLICT** and the course is left alone. Findings recurring across tracks are collapsed to one row listing all sites. OK claims are omitted. Every finding below is present identically in the DE file and its EN twin unless a row or the EN-drift note says otherwise.

---

## HIGH severity

| File(s) | Course says | Docs / ground truth say | Verdict | Sev |
|---|---|---|---|---|
| `agentowner/…/lessons/0003-approve-orders.html`, `n8n/…/lessons/0003-prepare-order.html`, `claude-code/…/lessons/0003-prepare-order.html` (DE+EN); MISSION/index inherit | The lesson finale presents the Messenger/WhatsApp approval — order details arriving with **Bestätigen/Ablehnen buttons** — as the working way to approve or reject; the builder capstones make the *reject-in-Messenger* step the finish line, run against production with no sandbox. | Docs (journeys/agentowner §7, architecture WhatsApp flow, messenger-adapter) show this delivery as functional. **Ground truth: Messenger/WhatsApp approval delivery is BROKEN on prod** — `prepare_order` claims a notification was sent, nothing arrives. The finale silently depends on it; the Claude Code learner is left holding a **real, orphaned `prepared` order in production** with no web-decline fallback taught. | MISLEADING-OMISSION | high |
| `agentowner/…/lessons/0004-pay-and-comply.html` (DE+EN); body + quiz | "Du zahlst nicht im Voraus. **Erst wenn ein Auftrag abgeschlossen und geschlossen ist**, stellt die Plattform dir dafür eine Rechnung." Quiz-correct: invoice arrives "erst nach dem Abschluss." | journeys/agentowner §8 (ZAG two-invoice): the platform brokerage-fee invoice is generated **at order assignment, not only at close** — assignment precedes completed/closed in the state machine. | COURSE-WRONG | high |
| `skillanbieter/…/lessons/0001-get-found.html` (DE+EN); body + quiz | To get found, the AI agent "matches on your words — **title, description, category, tags** — then ranks by relevance and your rating." Whole lesson coaches a rich WHAT/WHEN **description** + 3–8 tags + category; quiz rewards the richest *description*. | **Ground truth: `search_skillanbieter` matches LITERALLY** — an AND over substrings of skill **NAMES** (+ location text) only. Descriptions, tags, and category are **not** searched; no synonyms/word-forms. Discoverability rides on the literal words in the skill NAME — the lesson inverts the load-bearing rule. (Docs' tsvector/stemming happy-path is the SOURCE-CONFLICT below, so this is omission, not COURSE-WRONG.) | MISLEADING-OMISSION | high |

## MED severity

| File(s) | Course says | Docs / ground truth say | Verdict | Sev |
|---|---|---|---|---|
| `customer/…/lessons/0001-find-on-marketplace.html`, `n8n/…/lessons/0002-search.html`, `claude-code/…/lessons/0002-search.html` (DE+EN) | Teach a keyword / natural-language search ("Finde eine Reinigungskraft in Münster — zeig mir die besten 3") as a **reliable** way to find providers, sorted by distance/relevance — no caveat on how matching works. | Ground truth: literal AND-of-substrings over skill **names + location** only. Because the **model** picks the `query` arg, a compound noun like *Reinigungskraft* can map to a term that substring-fails and returns nothing (cf. the known misses `Einkaufshilfe`, `Workshop-Moderation`). Only the hand-picked base-word query is safe. | MISLEADING-OMISSION | med |
| Docs `mcp-guide.md`, `journeys/skillanbieter.md`, `database.md` (course untouched) | — (course does not state search mechanics) | Docs describe `search_skillanbieter.query` as "German full-text" / PostgreSQL `tsvector` with **German stemming**. Ground truth: literal substring match over names + location, no stemming. Docs overstate; this stale happy-path underlies every search finding above. | SOURCE-CONFLICT | med |
| `customer/…/lessons/0003-pay-securely.html` (DE+EN) | "Du bezahlst deinen Auftrag sicher über die Plattform … gegen eine Rechnung"; "die Fachkraft bekommt nie deine Bankdaten" — the whole order amount framed as flowing through the platform. | journeys/customer §7 (ZAG): the platform invoices only its **~15 % brokerage fee** through Stripe/PayPal; the **service amount settles directly customer↔provider, off-platform** (no platform custody). | MISLEADING-OMISSION | med |
| `agentowner/…/lessons/0004-pay-and-comply.html` (DE+EN) | "Datenschutz — unter **Einstellungen → Datenschutz** verwaltest du deine Einwilligungen"; primary-source callout invites finding it in the app. | journeys/agentowner §10 FLAG: **no dedicated consent page under `agentowner/settings`** — the consent UI ships only for the Skillanbieter (`/dashboard/settings/consent`); the Agentowner screen is not yet implemented. | COURSE-WRONG | med |
| `agentowner/…/lessons/0002-set-limits.html` (DE+EN) | Sells linking a messenger "damit dich Freigaben bequem **per Nachricht** erreichen" — the stated benefit is that approvals arrive by message. | Ground truth: linking (OTP → "Verknüpft") works, but the promised downstream benefit — approvals arriving in the messenger — is broken on prod. No caveat. | MISLEADING-OMISSION | med |
| `skillanbieter/…/lessons/0002-ready-for-money.html` (DE+EN) | The manual 4-type **Anbietertyp** (Privatperson / Kleinunternehmer / Gewerbetreibender / Freiberufler) is gone, replaced by an **auto-assigned 3-level Steuerprofil** (Einfach → Standard → Vollständig). | Sources disagree: `journeys/skillanbieter.md` steps 2 & 10 **still document** the manual 4-type flow; `database.md` (`tax_profiles.profile_level` = light/standard/full, `provider_type` enum = trader/private only) **backs** the lesson's auto 3-tier model. journeys.md is stale. | SOURCE-CONFLICT | med |
| `skillanbieter/…/lessons/0002-ready-for-money.html`, `GLOSSARY.md` (Steuerprofil) | Tier thresholds hardcoded: **Standard "ab 200 € Einnahmen"**, **Vollständig "ab der Freigrenze von 256 €"**. | No source doc states either number. `database.md` `earnings_summary` cites only a €22k Kleinunternehmer warning; 200 € / 256 € appear nowhere in the docs or ground truth. | UNVERIFIABLE | med |
| `GLOSSARY.md` (Agentowner entry) | Agentowner "gibt dessen Aufträge frei **(per Messenger)**" — Messenger presented as *the* approval channel. | journeys/agentowner §7: approval is **via WhatsApp _or_ web** (web: open order → Bestätigen). Ground truth: messenger delivery broken on prod → **web is the working path**; naming only Messenger could strand a learner. | MISLEADING-OMISSION | med |

## LOW severity

| File(s) | Course says | Docs / ground truth say | Verdict | Sev |
|---|---|---|---|---|
| `agentowner/…/lessons/0001-understand-your-agents.html`, `n8n/…/lessons/0004-track-and-limits.html` (DE+EN) | 401 / lost-token fix offered casually: "erstellst du deinem Agenten einfach einen neuen Token" — no caveat. | Ground truth known prod bug: **token rotation orphans existing orders** — an order created under the previous token returns "Order not found" / drops from `list_orders` after regenerating. | MISLEADING-OMISSION | low |
| `n8n/…/lessons/0004-track-and-limits.html` (DE+EN) | For a 401 where the token "fehlt, **stimmt nicht oder wurde widerrufen**", "die Plattform sagt **'Bearer token required'**". | Ground truth: `Bearer token required` appears **only when no token is sent**; an invalid/revoked token returns `{"error":"unauthorized"}` with no such message — the string is wrong for the wrong/revoked sub-cases the sentence enumerates. | COURSE-WRONG | low |
| `agentowner/…/lessons/0004-pay-and-comply.html` (DE+EN) | "Du zahlst über die Plattform — gegen **eine Rechnung**, per Karte (Stripe) oder PayPal." Single-invoice, pay-through-platform framing. | journeys/agentowner §8: two-invoice broker model — platform bills only its ~15 % fee; the service amount settles directly between the parties (not held/passed through). | MISLEADING-OMISSION | low |
| `agentowner/…/lessons/0004-pay-and-comply.html` (DE+EN) | Pay the invoice "per Karte (Stripe) **oder PayPal**" — PayPal as an equal invoice-payment option. | journeys/agentowner §8 names only **Stripe checkout** for the invoice-pay button; PayPal appears only in platform-level webhook docs, not as an agentowner invoice-pay path. | UNVERIFIABLE | low |
| `agentowner/…/lessons/0001-understand-your-agents.html` (DE+EN) | Token described as a "Schlüssel" you keep like a password; **no mention it expires**. | journeys/agentowner Troubleshooting + security.md: **tokens expire after 90 days** (NFR6). A silently-expiring token stops the agent. | MISLEADING-OMISSION | low |
| `skillanbieter/…/lessons/0004-get-paid.html` (DE+EN) | "Net or gross — your **Steuerprofil level** decides" — net/gross framed purely as a VAT matter. | journeys/skillanbieter §9: the Gutschrift net = **gross − processing fee** (`PROVIDER_PROCESSING_FEE_RATE`, default 3 %); a processing-fee deduction exists independent of VAT and is never mentioned. | MISLEADING-OMISSION | low |
| `n8n/…/lessons/0004-track-and-limits.html`, `claude-code/…/lessons/0004-track-and-limits.html` (DE+EN) | Order history lists **`approval_requested`** as a stage (`prepared → approval_requested → rejected`). | `architecture.md`'s status list **omits `approval_requested`** (uses `pending_approval`). Ground truth confirms `approval_requested` **is** a real wire state (`prepared → approval_requested → agent_cancelled`) — the course is correct, the doc list is stale. | SOURCE-CONFLICT | low |
| `n8n/…/lessons/0004-track-and-limits.html` + `0001-connect.html` (DE+EN) | Demo key "enger begrenzt (**10 pro Minute**)" and "der nur lesen darf" (read-only). | Docs give MCP 30/min + token 60/min but state **no** demo-specific 10/min cap and **no** read-only scope for the demo token (only that a public demo token exists). | UNVERIFIABLE | low |
| `claude-code/…/lessons/0001-connect.html` (DE+EN) | The platform docs "name a public demo key that **can only read** (no `prepare_order`)." | `mcp-guide.md` references a public demo token but states **no scope / read-only restriction**; its existence is documented, its scope is not. | UNVERIFIABLE | low |
| `GLOSSARY.md` (Steuerprofil / "Anbietertyp entfallen") | Tax classification "**nicht wählbar**, die Plattform setzt sie automatisch"; retires "Anbietertyp (entfallen)". | `database.md` backs the auto-tiered model, but `journeys/skillanbieter.md` still describes a user-chosen Anbietertyp screen. Docs are internally inconsistent; the glossary follows `database.md`. | SOURCE-CONFLICT | low |
| `claude-code/de/index.html` vs `claude-code/en/index.html` | DE overview marks lessons 2–4 "**(folgt)**" and does not link them, though all four DE lessons exist and are authored; EN links all four. | (Content-state inconsistency, not a platform-fact drift.) DE overview understates what is available. | COURSE-WRONG | low |

---

**Run header** — Sources: `lys-docs` course (34 in-scope DE files + 30 EN twins drift-checked = 64 files) vs `linkyourskill.ai/docs` (15 docs incl. all 5 journeys) + live-verified platform ground truth. Date: 2026-07-17. Tracks: customer · agentowner · skillanbieter · n8n · claude-code + shared top-level. Method: five per-track readers + one shared-file reader, read-only. No lesson file modified.
